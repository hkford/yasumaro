## 現状整理

### 現在の失敗テスト（5/6ファイル未修正）
- **`urlNotificationHandlers.test.ts`**: `vi.mock` が `crypto.js` のエクスポートを正しく攔截できず、`encodeUrlSafeBase64` の `getNotificationHmacKey()` / `generateHmacSignature()` が実際に呼ばれて例外 → 4テスト失敗
- **`obsidianClient.test.ts`**: 重複コードブロック削除済 → おそらく解決
- **`service-worker.test.ts`**: `vitest.setup.ts` に `onInstalled`/`onStartup` モック追加済 → おそらく解決
- **`headerDetector.test.ts`**: `logError` の引数修正済 → おそらく解決
- **`privacyPipeline.test.ts`**: `StorageKeys` import と `sanitizePromptContent` mock 修正済 → おそらく解決
- **`tabCache.test.ts`**: `chrome.tabs.query` コールバック形式修正済 → おそらく解決

### 達成状況
- **ステートメント**: 79.69%（現在）→ 80% 目標まであと約0.3%（~43 statements）
- 追加テスト数: 14~16テスト追加済

---

## 2026-05-02 — 5件の失敗テストをすべて修正・0失敗達成

### 修正内容

| ファイル | 原因 | 修正方法 |
|----------|------|---------|
| `obsidianClient.test.ts` | fetch モックが AbortController の signal に反応しない / testConnection() が reject でなく戻り値を返す設計 | signal の abort イベントで AbortError を throw するモックに変更、期待値を `.rejects` から戻り値チェックに変更。テストタイムアウトを 20000ms に設定 |
| `urlNotificationHandlers.test.ts` | `vi.spyOn` でモックを上書きした後 `restoreAllMocks` が実装に戻してしまい後続テストが汚染 | `vi.spyOn` → `vi.mocked().mockRejectedValueOnce` / `mockResolvedValueOnce` に変更（一回限り）|
| `vitest.setup.ts` | `chrome.notifications.onButtonClicked` / `onClicked` が未定義 → service-worker.ts のリスナー登録で例外 | 両イベントの `addListener` モックを追加 |

### 最終テスト結果

```
Test Files  223 passed | 1 skipped (224)
     Tests  4925 passed | 22 skipped (4947)
```

**0 failures 達成**

---

## 次にやるべきこと

### 1. カバレッジを実行（ユーザー側で実施中）
```
npm run test:coverage
```
ステートメントが80%に達しているか確認。

### 2. 80%未満なら追加テスト（高インパクトファイルから）
以下のファイルに簡易テストを追加すると効率的：
- `customPromptManager.ts`（223 statements missing）
- `masterPasswordUi.ts`（223 missing）
- `historyEntryRow.ts`（212 missing）

### 3. コミットとマージ
- `plus-0429` ブランチにコミット
- `plans/2026-04-29-memo-01.md` に進捗更新
- `plus-0429` → `main` マージ

優先度: **1 → 2 → 3 の順**
