# PBI-13 手動検証チェックリスト

## 準備

```bash
npm run build
```

Chrome で `chrome://extensions` → 開発者モード ON → `dist/chromium-mv3` を読み込み（既存の拡張機能がある場合は更新）

---

## 1. 基本表示（全環境共通）

| # | 操作 | 期待結果 | 実施結果 |
|---|------|---------|---------|
| 1-1 | ダッシュボードを開く | 左ナビに「Diagnostics」タブが表示される | OK |
| 1-2 | Diagnostics タブをクリック | 7セクション + デバッグモードトグルが表示される | OK |
| 1-3 | **不足診断セクション**を確認 | 「不足なし — 全機能が有効です。✓」または環境に応じた不足項目が表示される | 下記に |
| 1-4 | **SQLite データベースセクション**を確認 | Status / Path / Fallback Mode / FTS5 Search / Source の5行が表示される | 下記に |
| 1-5 | ストレージ統計を確認 | KB数・URL件数が表示される | OK |
| 1-6 | 拡張機能情報を確認 | バージョン番号・拡張機能名が表示される | OK  |

### 1-3の結果

不足診断
FTS5: 利用不可 (OPFS 同期ビルド) [低]
LIKE 検索で代替動作中。FTS5 を有効にする場合は wa-sqlite を再ビルドしてください。

### 1-4の結果

SQLite データベース
状態
利用可能
パス
OPFS:/yasumaro-opfs/yasumaro.db
フォールバックモード
いいえ（ネイティブ SQLite）
FTS5 検索
✗ Not available (LIKE fallback)
ソース
opfs-worker



---

## 2. デバッグモード切替

| # | 操作 | 期待結果 | 実施結果 |
|---|------|---------|---------|
| 2-1 | デバッグモードトグルを確認 | デフォルトはOFF。コンパイルオプション折りたたみが**非表示** | ok |
| 2-2 | トグルをONにクリック | コンパイルオプション折りたたみセクションが**表示**される | ok |
| 2-3 | ページをリロード | デバッグモードが**ONのまま維持**される | OK |
| 2-4 | トグルをOFFに戻す | コンパイルオプション折りたたみが**非表示**に戻る |OK |

---

## 3. コンパイルオプション表示（デバッグモードON時）

| # | 操作 | 期待結果 | 実施結果 |
|---|------|---------|---------|
| 3-1 | 「Compile Options」折りたたみを開く | Source（idb/opfs-worker）・Total（件数）・FTS/VFS 関連が表示される | OK |
| 3-2 | 「All N options」折りたたみを開く | 全コンパイルオプションがmonospaceフォントで表示される | OK |

---

## 4. 不足診断（環境に応じて）

| # | 操作 | 期待結果 | 実施結果 |
|---|------|---------|---------|
| 4-1 | OPFS が利用可能な環境で表示 | 不足なし（✓） | OK |
| 4-2 | SQLite テストボタンをクリック | 「✓ SQLite は正常に動作しています。(FTS5 ✓ or LIKE fallback)」 | ✓ SQLite は正常に動作しています。 (LIKE fallback) |
| 4-3 | FTS5 が無効な環境（手動テスト） | 「FTS5: 利用不可」＋再ビルド指示が表示される | FTS5 検索
✗ Not available (LIKE fallback) とでた |

---

## 5. 乖離警告

| # | 操作 | 期待結果 | 実施結果 |
|---|------|---------|---------|
| 5-1 | 通常環境（OPFS Worker 動作中） | 警告は**非表示** | OK |
| 5-2 | 特殊環境（dashboard と offscreen が異なる戦略） | 「環境判定に乖離があります」警告が表示される | N/A（通常環境で確認済み） |

---

## 6. 既存機能への影響なし

| # | 操作 | 期待結果 | 実施結果 |
|---|------|---------|---------|
| 6-1 | SQLite テストボタン | 従来通り動作（✓ or ✗ を表示） | ✓ SQLite は正常に動作しています。 (LIKE fallback) |
| 6-2 | OPFS スパイクボタン | 従来通り動作（✓/✗ + ステップ一覧） | Spike failed. |
| 6-3 | 変換ボタン | 従来通り動作（確認ダイアログ → 実行 → read/inserted/total 表示） | ✓ 変換が完了しました。 read=0 inserted=0 total=1170 |
| 6-4 | Obsidian テストボタン | 従来通り動作（✓/✗ + メッセージ） | Obsidian: ✓ Success! Connected to Obsidian. Settings Saved. |
| 6-5 | AI テストボタン | 従来通り動作（✓/✗ + メッセージ） | AI: ✓ Connected to AI API. |

---

## コンソール確認

| # | 操作 | 期待結果 | 実施結果 |
|---|------|---------|---------|
| C-1 | Chrome DevTools のコンソールを開く | 重大なエラーが表示されない | OK |
| C-2 | `PRAGMA compile_options` のログを確認（**Offscreen Document コンソール**で確認） | `SQLite compile options:` でFTS/VFS関連がログ出力される | OK（FTS5 は同期ビルドに未同梱のため出力なし = 既知の制限） |

---

## 実施結果メモ

- 実施日: 2026-06-15 11:54
- 環境: MBA M1 16GB


