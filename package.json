{
  "name": "obsidian-weave",
  "version": "5.2.2",
  "description": "Smartly backup closed tabs to Obsidian with AI summaries and context linking.",
  "type": "module",
  "scripts": {
    "dev": "wxt",
    "build": "node scripts/check-version-consistency.js && wxt build",
    "build:watch": "wxt build --watch",
    "update-preset": "node --loader ts-node/esm scripts/update-preset-domains.ts",
    "update-manifest": "npx ts-node scripts/update-manifest-from-preset.ts",
    "update-tranco": "npm run update-preset && npm run update-manifest",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ci": "xvfb-run --auto-servernum --server-args='-screen 0 1280x960x24' playwright test --grep @extension",
    "type-check": "tsc --noEmit",
    "type-check:watch": "tsc --noEmit --watch",
    "type-check:test": "tsc --project tsconfig.test.json --noEmit",
    "test:type-safe": "npm run type-check:test && npm test",
    "lint": "tsc --noEmit",
    "validate": "npm run type-check && npm test",
    "docs": "typedoc",
    "docs:watch": "typedoc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "obsidian",
    "chrome-extension",
    "browser-history",
    "ai-summary"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@peculiar/webcrypto": "^1.5.0",
    "@playwright/test": "^1.58.2",
    "@rollup/plugin-commonjs": "^29.0.2",
    "@rollup/plugin-node-resolve": "^16.0.3",
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tailwindcss/vite": "^4.2.2",
    "@types/chrome": "^0.1.36",
    "@types/node": "^25.2.3",
    "@vitest/coverage-v8": "^4.1.4",
    "adm-zip": "^0.5.16",
    "css-tree": "^3.2.1",
    "happy-dom": "^20.9.0",
    "jsdom": "^29.0.0",
    "svelte": "^5.55.4",
    "tailwindcss": "^4.2.2",
    "ts-node": "^10.9.2",
    "typedoc": "^0.28.19",
    "typescript": "^5.9.3",
    "vite": "^8.0.8",
    "vitest": "^4.1.4",
    "wxt": "^0.20.25"
  },
  "dependencies": {
    "bloomfilter": "1.1.0"
  },
  "overrides": {
    "htmlparser2": "^12.0.0"
  },
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=10.0.0"
  }
}
